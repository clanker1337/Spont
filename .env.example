# Server-side provider keys (never exposed to the browser).
# Copy to .env and fill in. The app runs in demo mode with these unset.

# Google (Places New, Geocoding, Routes) — server only
GOOGLE_PLACES_API_KEY=
GOOGLE_GEOCODING_API_KEY=
GOOGLE_ROUTES_API_KEY=
